- unless video.new_record?
  %li
    = link_to video.title, video.video.url, target: '_blank'
    = link_to video_path(video), method: :delete, data: {confirm: 'Are you sure?'}, class: 'delete-button' do
      %i.fa.fa-trash-o