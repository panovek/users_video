- if user_signed_in?
  %li
    = link_to destroy_user_session_path, :method=>'delete' do
      = image_tag current_user.avatar_url, class: 'avatar pull-left'
      Sign out
- else
  %li= link_to 'Sign in', user_omniauth_authorize_path(:facebook)